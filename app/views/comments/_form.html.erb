<div class="comment_area">
  <%= form_for([@location, @location.comments.build]) do |f| %>
    <%= f.text_area :content, class: "form-control", rows: 6, placeholder: "Add A Comment..." %>
    <%= f.submit "Reply", class: "btn btn-primary" %>

    <% end %>

</div>